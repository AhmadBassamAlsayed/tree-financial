# Server Configuration
NODE_ENV=development
PORT=3004

# Database Configuration
DB_HOST=localhost
DB_PORT=3306
DB_NAME=tree_financial
DB_USER=root
DB_PASSWORD=meandmylife2005

# SSO Service URL
SSO_BASE_URL=http://localhost:3001

# Internal Microservice Secret
# Must match INTERNAL_API_SECRET on all services
INTERNAL_API_SECRET=your_internal_secret_here

# Shops Service URL (used for shop ownership verification)
SHOPS_BASE_URL=http://localhost:3003

# Secret used to sign/verify ledger entry fingerprints
# Keep this separate and highly protected — changing it invalidates all fingerprints
FINGERPRINT_SECRET=your_fingerprint_secret_here
